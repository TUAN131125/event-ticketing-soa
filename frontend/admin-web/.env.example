VITE_ESB_API_URL=http://localhost:8000
# Realtime uses REST polling until a WebSocket route is exposed by the public ESB edge.
# Set true after the expanded ESB cancel contract accepts a JSON reason body.
VITE_ESB_CANCEL_REASON_ENABLED=false
