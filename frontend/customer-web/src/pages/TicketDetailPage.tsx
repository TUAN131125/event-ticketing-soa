import { Link, useParams } from 'react-router-dom';
import { Download, Ticket as TicketIcon } from 'lucide-react';
import { Badge, Button, Card, EmptyState, Spinner } from '@event-ticketing/shared-ui';
import { useTicket } from '../app/hooks';
import { QueryState } from './PageState';
export function TicketDetailPage() { const { ticketId } = useParams(); const result = useTicket(ticketId); if (result.isLoading) return <section className="container page-section page-state"><Spinner label="Loading ticket" /></section>; if (result.isError || !result.data) return <QueryState error={result.error} retry={() => void result.refetch()} />; const ticket = result.data; return <section className="container page-section narrow-page"><Link to={`/bookings/${ticket.bookingId}`} className="back-link">← Booking</Link><Card className="ticket-card"><div className="ticket-top"><div className="result-icon"><TicketIcon size={26} /></div><div><p className="eyebrow">Digital ticket</p><h1>{ticket.event?.name || ticket.ticketId}</h1><Badge tone={ticket.status === 'VALID' ? 'success' : 'warning'}>{ticket.status}</Badge></div></div>{ticket.qrCode ? <div className="ticket-code"><img src={ticket.qrCode} width="180" height="180" alt="Ticket QR code" /><span>{ticket.seatLabel || 'Seat assigned at venue'}</span></div> : <EmptyState title="QR code not available yet" description="Your ticket code will appear after the booking service confirms payment." />}<Button variant="secondary" onClick={() => window.print()}><Download size={16} /> Print ticket</Button></Card></section>; }
