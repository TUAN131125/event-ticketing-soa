{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://event-ticketing.example/contracts/events/ticket-issued.schema.json",
  "title": "ticket.issued",
  "type": "object",
  "additionalProperties": false,
  "required": ["eventId", "eventType", "aggregateType", "aggregateId", "aggregateVersion", "correlationId", "occurredAt", "payload"],
  "properties": {
    "eventId": {"type": "string", "format": "uuid"},
    "eventType": {"const": "ticket.issued"},
    "aggregateType": {"const": "Ticket"},
    "aggregateId": {"$ref": "#/$defs/identifier"},
    "aggregateVersion": {"const": 1},
    "correlationId": {"$ref": "#/$defs/identifier"},
    "occurredAt": {"type": "string", "format": "date-time"},
    "payload": {"$ref": "#/$defs/issuedPayload"}
  },
  "$defs": {
    "identifier": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"},
    "issuedPayload": {
      "type": "object",
      "additionalProperties": false,
      "required": ["ticketId", "bookingId", "customerId", "eventId", "paymentId", "seatId", "seatLabel", "ticketType", "status", "qrVersion", "resourceVersion", "occurredAt"],
      "properties": {
        "ticketId": {"$ref": "#/$defs/identifier"},
        "bookingId": {"$ref": "#/$defs/identifier"},
        "customerId": {"$ref": "#/$defs/identifier"},
        "eventId": {"$ref": "#/$defs/identifier"},
        "paymentId": {"$ref": "#/$defs/identifier"},
        "seatId": {"$ref": "#/$defs/identifier"},
        "seatLabel": {"$ref": "#/$defs/identifier"},
        "ticketType": {"$ref": "#/$defs/identifier"},
        "status": {"const": "VALID"},
        "qrVersion": {"const": 1},
        "resourceVersion": {"const": 1},
        "occurredAt": {"type": "string", "format": "date-time"}
      }
    }
  }
}
